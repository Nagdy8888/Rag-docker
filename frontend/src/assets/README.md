# frontend/src/assets

Static assets used by the React app (images, icons, fonts).

Import from here in components (e.g. `import logo from '@/assets/logo.svg'`). Vite will process and hash them for production.
