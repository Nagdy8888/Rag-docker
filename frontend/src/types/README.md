# frontend/src/types

TypeScript types and interfaces shared across the frontend.

- **index.ts** — Central export of types (e.g. Message, Session, Document, API response shapes).

Import from `@/types` or `../types` as needed.
