# backend/scripts

Utility and one-off scripts for the backend.

Use this folder for migration helpers, seed data, or local dev scripts. The main app entry point is in [../app/main.py](../app/main.py).
